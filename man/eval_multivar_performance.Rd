% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/eval_multivar_performance.R
\name{eval_multivar_performance}
\alias{eval_multivar_performance}
\title{Summarize multivar performance against simulation truth (robust to missing parts)}
\usage{
eval_multivar_performance(
  sim_obj,
  fit_obj,
  eps = 1e-08,
  reduced.output = TRUE,
  averages.only = TRUE,
  label = NULL
)
}
\arguments{
\item{sim_obj}{Output of multivar_sim()}

\item{fit_obj}{Output of cv.multivar() / cv.fused() (or similar list with $mats)}

\item{eps}{Small positive constant to stabilize relative errors}

\item{reduced.output}{If TRUE, keep a reduced column set}

\item{averages.only}{If TRUE, return only summary rows (unique_mean, total_mean)
and the single 'common' row (if present)}

\item{label}{Optional label for the fitted model; by default, uses the
deparsed name of `fit_obj`}
}
\description{
Compare estimated transition matrices to simulation truth, but only for
components that actually exist in both `sim_obj` and `fit_obj`. For example,
if `fit_obj$mats` only contains `total`, only "total" rows are returned.
}
