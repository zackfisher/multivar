% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/maity_debiasing.R
\name{estimate_maity_effects}
\alias{estimate_maity_effects}
\title{Estimate initial coefficients using Maity et al. (2022) MrLasso approach}
\usage{
estimate_maity_effects(
  Ak,
  bk,
  d,
  k,
  nfolds,
  lambda_choice,
  eta = 2,
  thresh_const = 1
)
}
\arguments{
\item{Ak}{List of design matrices per subject (each n_k x d)}

\item{bk}{List of response matrices per subject (each n_k x d)}

\item{d}{Number of variables}

\item{k}{Number of subjects}

\item{nfolds}{Number of CV folds for LASSO fits}

\item{lambda_choice}{Which lambda from cv.glmnet ("lambda.min" or "lambda.1se")}

\item{eta}{Numeric. Scale parameter for re-descending loss. Default 2.0.}

\item{thresh_const}{Numeric. Multiplicative constant for threshold rates. Default 1.0.}
}
\value{
List with same structure as \code{\link{decompose_effects}}
}
\description{
Five-step pipeline:
\enumerate{
  \item Per-subject LASSO via \code{\link{estimate_raw_effects}}
  \item Debias each subject via nodewise regression
  \item Aggregate common effects via re-descending loss
  \item Soft-threshold common at rate sqrt(log(d^2) / (k * min(n_k)))
  \item Unique = debiased - common_dense, thresholded at sqrt(log(d^2) / n_k)
}
}
\keyword{internal}
